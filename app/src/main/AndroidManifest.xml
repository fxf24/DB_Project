<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test.db_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Staff_Login" />
        <activity android:name=".Staff_Start" />
        <activity android:name=".Staff_Reservation" />
        <activity android:name=".Staff_Room" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Customer_start" />
        <activity android:name=".Customer_Reservation" />
        <activity android:name=".Customer_Reservation_Check" />
        <activity android:name=".Customer_Claim" />
        <activity android:name=".Customer_Reservation_inform" />
        <activity android:name=".Customer_Reservation_Room"></activity>
    </application>

</manifest>